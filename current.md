# 《时层钻探局》实施状态

更新时间：2026-08-11（M0/M1 实施中）

## 范围

- 当前任务：完成 M0 工程初始化与验证骨架，随后完成 M1 可破坏地形技术尖峰；不实施 M2。
- 权威规格已阅读：`DEVELOPMENT_PLAN.md`、`CONTENT_BIBLE.md`、`TECHNICAL_TEST_SPEC.md`。
- Git：已在空工程初始化；权威规格文档保持未跟踪且不纳入提交。

## 已完成

- Godot 项目基础配置、Autoload 服务、状态机、存档/设置骨架、灰盒场景和中英文翻译资源。
- M1 整数地形真相源、32×32 分块、同帧去重、爆破上限、每物理帧两个碰撞分块预算、无幽灵碰撞查询与对象池；固定场景包含 500 活跃瓦片、20 环境占位和 3 条回声占位。
- 原生测试运行器和地形单元测试、M1 固定基准脚本。

## 接下来

1. 提交并推送本轮 M0/M1 修复与验证改进。
2. 停止于 M1；不实施 M2。

## 验证状态

- M0：已提交 `d26ea83`。解析、7 个测试及本地化校验均通过。
- M1 实际性能基准：10 秒预热、120 秒采样；工作帧平均 0.0053 ms、P95 0.004 ms、P99 0.091 ms、地形重建峰值 0.208 ms、内存变化 0.37 MB（Windows 开发机，Ryzen 7 7800X3D，headless）。Windows 工作时间阈值通过；headless 调度间隔 P95 为 20.706 ms，不作为渲染帧性能。
- 耐久：真实连续 600 秒通过，28,988 个 headless tick、24,000 块删除、1,200 次重建、对象池 200 个可用。
- 场景静态加载工具不适用于 Autoload（在 `--script` 模式缺失 Autoload 标识符）；已改为真实项目启动后带 `--scene-smoke` 自动退出检查。
- 真实项目启动检查：通过。启动项目后加载并实例化 6 个灰盒场景；Boot 的立即切场景错误已修复。
- M1：已提交；场景切换修复将以普通后续提交推送，不改写远程历史。其余 M1 的单元、性能与耐久验证均已完成。

## 本轮补充验证

- 真实场景 smoke：通过，启动项目后加载并实例化 6 个场景。
- 测试运行器自测：故意失败、零用例、钩子、受控失败和超时均按预期退出；正常单元测试 10/10 通过。
- 统一验证脚本：通过，覆盖场景启动、运行器自测、全部单元测试、本地化和性能冒烟。
