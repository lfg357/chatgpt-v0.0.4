# 《时层钻探局》实施状态

更新时间：2026-08-12（M0/M1 验证整改中）

## 范围

- 当前任务：完成 M0 工程初始化与验证骨架，随后完成 M1 可破坏地形技术尖峰；不实施 M2。
- 权威规格已阅读：`DEVELOPMENT_PLAN.md`、`CONTENT_BIBLE.md`、`TECHNICAL_TEST_SPEC.md`。
- Git：已在空工程初始化；权威规格文档保持未跟踪且不纳入提交。

## 已完成

- Godot 项目基础配置、Autoload 服务、状态机、存档/设置骨架、灰盒场景和实际注册到 TranslationServer 的中英文翻译资源。
- M1 整数地形真相源、32×32 分块、同帧去重、爆破上限、每物理帧两个碰撞分块预算、无幽灵碰撞查询与对象池；固定场景包含 500 活跃瓦片、20 环境占位和 3 条回声占位。
- 原生测试运行器和地形单元测试、M1 固定基准脚本。

## 接下来

1. 完成 OA-013 的无 Godot 类缓存干净快照验证。
2. 完成 OA-014 的真实连续 30 分钟耐久与内存增长门禁。
3. 提交并推送本轮整改；停止于 M1，不实施 M2。

## 验证状态

- M0：已提交 `d26ea83`。解析、7 个测试及本地化校验均通过。
- M1 实际性能基准：10 秒预热、120 秒采样；工作帧平均 0.0053 ms、P95 0.004 ms、P99 0.091 ms、地形重建峰值 0.208 ms、内存变化 0.37 MB（Windows 开发机，Ryzen 7 7800X3D，headless）。Windows 工作时间阈值通过；headless 调度间隔 P95 为 20.706 ms，不作为渲染帧性能。
- 耐久：真实连续 600 秒通过，28,988 个 headless tick、24,000 块删除、1,200 次重建、对象池 200 个可用。
- 场景静态加载工具不适用于 Autoload（在 `--script` 模式缺失 Autoload 标识符）；已改为真实项目启动后带 `--scene-smoke` 自动退出检查。
- 真实项目启动检查：通过。启动项目后加载并实例化 6 个灰盒场景；Boot 的立即切场景错误已修复。
- M1：已提交；场景切换修复将以普通后续提交推送，不改写远程历史。其余 M1 的单元、性能与耐久验证均已完成。

## 本轮补充验证

- 真实场景 smoke：通过，启动项目后加载并实例化 6 个场景。
- 测试运行器自测：故意失败、零用例、钩子、受控失败、同步与异步超时均按预期退出；正常单元测试 12/12 通过。
- 统一验证脚本：通过，覆盖场景启动、运行器自测、全部单元测试、本地化和性能冒烟。

## QA 修复（2026-08-11）

- QA-009：`test_core` 释放临时 `AppState`，正常 `--verbose` 套件 12/12 通过且无 ObjectDB/resource leak 诊断。
- QA-011：`verify.ps1` 默认执行完整 10 秒预热/120 秒性能门禁；`-Quick` 仅开发冒烟。基准显式比较 Windows/Web P95、P99、重建与内存阈值，并有超预算非零自测。
- QA-012：运行器现将不可实例化脚本、运行时测试错误和钩子错误计为失败；自测覆盖加载失败、运行时错误、钩子清理及同步/异步超时。

## OA 整改（2026-08-12）

- OA-013：所有被自动发现的测试及自测夹具改为显式继承 `res://tests/test_case.gd`，不再依赖 Godot 编辑器生成的全局 `class_name` 缓存；从 `git archive HEAD` 生成的纯快照在无 `.godot` 缓存下执行 `verify.ps1 -Quick` 已通过。
- OA-014：耐久脚本默认改为 1,800 秒，记录起止 `MEMORY_STATIC`、计算增长百分比并执行 Windows ≤10% / Web ≤15% 门禁；`verify.ps1` 的默认全量路径已纳入此项，并增加强制越限自测。真实连续 1,800 秒于 Windows/headless 通过：起始 24.394 MB、结束 24.431 MB、增长 0.153%（≤10%）、86,959 帧、65,536 块移除、3,277 次重建、对象池 200 可用。
- QA-015：冷启动复现确认 `--script` 不会保证编辑器的全局类缓存，导致 Autoload 的 `Result`、`ContentDef`、`SaveSnapshot`、`AppSettings` 解析失败。核心与地形依赖已改为显式 `preload`，并新增 Autoload 解析门禁及其非零自测；后续 QA-016 发现静态工厂仍有自 `class_name` 引用，正在移除并复验。
- QA-016：`Result` 和 `SaveSnapshot` 的静态工厂在冷启动仍通过自身 `class_name` 作为返回类型/构造器，导致核心 Autoload 连锁失败。已改为按资源路径动态构造，且 `verify.ps1` 将正常测试套件置于首阶段，防止此前解析门禁生成缓存后掩盖冷启动失败；两个独立纯 Git 快照验证均通过：首个 Godot 进程运行测试套件为 12/12、退出码 0，完整 `verify.ps1 -Quick` 亦通过。

## M1 人工验收入口（完成）

- 反馈：下潜场景此前只显示菜单式“结算”按钮，M1 可破坏地形仅存在于自动化基准与单元测试，无法由人工验收。
- 处理：已将下潜场景替换为灰盒地形试验场；左键单瓦片破坏、右键半径爆破、实时显示待提交/脏分块/碰撞重建计数，并保留返回基地。该入口不包含 M2 的正式钻探移动或手感。场景单元测试、实际场景启动、本地化和统一快速验证均已通过。
